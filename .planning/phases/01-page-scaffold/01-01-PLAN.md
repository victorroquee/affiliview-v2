---
phase: 01-page-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/CpaVariavel.tsx
  - src/App.tsx
  - src/components/Sidebar.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "Sidebar shows a clickable 'CPA Variavel' entry with a Calculator icon"
    - "Clicking the sidebar entry renders the CPA Variavel page"
    - "Navigating away from CPA Variavel and back works without errors"
    - "The CPA Variavel button is no longer locked/disabled"
  artifacts:
    - path: "src/pages/CpaVariavel.tsx"
      provides: "CPA Variavel page component"
      exports: ["default"]
    - path: "src/App.tsx"
      provides: "Page routing including cpa-variavel"
      contains: "cpa-variavel"
    - path: "src/components/Sidebar.tsx"
      provides: "Sidebar with unlocked CPA Variavel nav button"
      contains: "onNavigate(\"cpa-variavel\")"
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "src/App.tsx"
      via: "onNavigate('cpa-variavel') sets page state"
      pattern: "onNavigate.*cpa-variavel"
    - from: "src/App.tsx"
      to: "src/pages/CpaVariavel.tsx"
      via: "conditional render when page === 'cpa-variavel'"
      pattern: "page === .cpa-variavel."
---

<objective>
Wire the new CPA Variavel page into the application: create the page component, add it to App.tsx routing, and unlock the sidebar navigation button.

Purpose: Users can navigate to the CPA Variavel page from the sidebar (UX-01). This is the scaffold that Phase 2 builds upon.
Output: Three modified/created files that complete page navigation wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing Page type and Sidebar props the executor must extend -->

From src/App.tsx:
```typescript
type Page = "dashboard" | "affiliates" | "calculator" | "cpa-fixo" | "mail";
```

From src/components/Sidebar.tsx:
```typescript
type Page = "dashboard" | "affiliates" | "calculator" | "cpa-fixo" | "mail";

interface SidebarProps {
  activePage: Page;
  onNavigate: (page: Page) => void;
}
```

From src/pages/CpaFixo.tsx (pattern to follow for props):
```typescript
interface Props {
  filteredRows: TransactionRow[];
  loading: boolean;
}
const CpaFixo: React.FC<Props> = ({ filteredRows, loading }) => { ... }
```

Existing sidebar CPA Variavel button (currently locked/disabled):
```tsx
<button
  className="sidebar-link sidebar-link--locked"
  disabled
  title="Funcionalidade em desenvolvimento"
>
  <Calculator size={15} strokeWidth={1.4} />
  CPA Variavel
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CPA Variavel page scaffold and wire into App.tsx routing</name>
  <files>src/pages/CpaVariavel.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (current Page type, import pattern, conditional render chain)
    - src/pages/CpaFixo.tsx (first 20 lines — component signature and props pattern)
  </read_first>
  <action>
1. Create `src/pages/CpaVariavel.tsx`:
   - Import React, Calculator icon from lucide-react, LoadingDot from "../components/LoadingDot", TransactionRow type from "../lib/transactions"
   - Define Props interface: `{ filteredRows: TransactionRow[]; loading: boolean; }`
   - Export default React.FC component named `CpaVariavel`
   - If loading, render `<LoadingDot />` (same pattern as CpaFixo)
   - Otherwise render a container div with className "cpa-variavel-page" containing:
     - An h2 header: "CPA Variavel"
     - A paragraph: "Pagina em construcao — dados e simulacao virao nas proximas fases."
   - This is a placeholder body — Phase 2 replaces the content

2. Update `src/App.tsx`:
   - Change the Page type to: `type Page = "dashboard" | "affiliates" | "calculator" | "cpa-fixo" | "cpa-variavel" | "mail";`
   - Add import: `import CpaVariavel from "./pages/CpaVariavel";`
   - In the conditional render chain (the ternary in page-body), add a case BEFORE the mail case:
     ```
     ) : page === "cpa-variavel" ? (
       <CpaVariavel
         filteredRows={filteredRows}
         loading={loading}
       />
     ```
   - Ensure the ternary order is: dashboard -> affiliates -> cpa-fixo -> cpa-variavel -> mail -> calculator(default)
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/Affiliview-v3 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File src/pages/CpaVariavel.tsx exists and exports a default React.FC
    - src/pages/CpaVariavel.tsx contains `filteredRows: TransactionRow[]` in its Props
    - src/pages/CpaVariavel.tsx contains `loading: boolean` in its Props
    - src/pages/CpaVariavel.tsx contains `<LoadingDot` for the loading state
    - src/App.tsx Page type includes `"cpa-variavel"`
    - src/App.tsx contains `import CpaVariavel from "./pages/CpaVariavel"`
    - src/App.tsx contains `page === "cpa-variavel"` in the render chain
    - src/App.tsx renders `<CpaVariavel` with `filteredRows={filteredRows}` and `loading={loading}`
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>CpaVariavel page component exists with correct props, App.tsx routes to it when page state is "cpa-variavel", TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Unlock CPA Variavel sidebar button and wire navigation</name>
  <files>src/components/Sidebar.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (current locked button markup and Page type)
  </read_first>
  <action>
1. Update the Page type in Sidebar.tsx to match App.tsx:
   `type Page = "dashboard" | "affiliates" | "calculator" | "cpa-fixo" | "cpa-variavel" | "mail";`

2. Replace the locked CPA Variavel button (lines 46-53) with an active navigation button following the exact pattern of the CPA Fixo button above it:
   ```tsx
   <button
     className={`sidebar-link ${activePage === "cpa-variavel" ? "active" : ""}`}
     onClick={() => onNavigate("cpa-variavel")}
   >
     <Calculator size={15} strokeWidth={1.4} />
     CPA Variavel
   </button>
   ```

3. Remove the `disabled` attribute and `title="Funcionalidade em desenvolvimento"` — the button is no longer locked.
4. Remove the `sidebar-link--locked` class — use the standard `sidebar-link` class with the active conditional.
5. Keep the Calculator icon import (already imported at line 2).
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/Affiliview-v3 && npx tsc --noEmit 2>&1 | head -10 && grep -c "sidebar-link--locked" src/components/Sidebar.tsx && grep -c 'onNavigate("cpa-variavel")' src/components/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Sidebar.tsx Page type includes "cpa-variavel"
    - src/components/Sidebar.tsx contains `onNavigate("cpa-variavel")`
    - src/components/Sidebar.tsx contains `activePage === "cpa-variavel"`
    - src/components/Sidebar.tsx does NOT contain "sidebar-link--locked"
    - src/components/Sidebar.tsx does NOT contain `disabled`
    - src/components/Sidebar.tsx does NOT contain "Funcionalidade em desenvolvimento"
    - src/components/Sidebar.tsx still imports Calculator from lucide-react
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>Sidebar CPA Variavel button is clickable, triggers navigation to "cpa-variavel" page, shows active state when selected, and the locked styling is removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only adds client-side routing — no new data flows or API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Page type union | accept | Page state is client-only, no auth boundaries — invalid values fall through to default (Calculator) via existing ternary |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. In the running app, sidebar shows "CPA Variavel" as a clickable (not grayed out) button
4. Clicking "CPA Variavel" renders the placeholder page with "CPA Variavel" heading
5. Navigating to other pages and back to CPA Variavel works without console errors
</verification>

<success_criteria>
- UX-01 is satisfied: "CPA Variavel" sidebar entry with Calculator icon is clickable and navigates to the page
- Page renders without errors or error boundaries triggering
- No TypeScript compilation errors
- Build succeeds for Vercel deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-page-scaffold/01-01-SUMMARY.md`
</output>
