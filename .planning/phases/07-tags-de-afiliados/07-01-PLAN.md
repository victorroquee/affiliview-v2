---
phase: 07-tags-de-afiliados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useAffiliateTags.ts
  - src/components/AffiliateDrawer.tsx
autonomous: true
requirements:
  - TAG-01
must_haves:
  truths:
    - "User can type a tag name in the drawer and press Enter to add it to an affiliate"
    - "User can see assigned tags as removable chips in the drawer"
    - "User can click the X on a tag chip to remove it"
    - "Tags persist across page refresh via localStorage"
  artifacts:
    - path: "src/hooks/useAffiliateTags.ts"
      provides: "localStorage-backed tag management hook"
      exports: ["useAffiliateTags"]
    - path: "src/components/AffiliateDrawer.tsx"
      provides: "Tag assignment UI section in affiliate drawer"
      contains: "useAffiliateTags"
  key_links:
    - from: "src/hooks/useAffiliateTags.ts"
      to: "localStorage"
      via: "getItem/setItem with key affiliview-affiliate-tags"
      pattern: "affiliview-affiliate-tags"
    - from: "src/components/AffiliateDrawer.tsx"
      to: "src/hooks/useAffiliateTags.ts"
      via: "useAffiliateTags hook import"
      pattern: "useAffiliateTags"
---

<objective>
Create the useAffiliateTags hook for localStorage-backed tag management and integrate tag assignment UI into the AffiliateDrawer component.

Purpose: Enable users to assign and remove tags on any affiliate, persisted in localStorage.
Output: Working hook + drawer tag section where users can add/remove tags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tags-de-afiliados/07-CONTEXT.md

@src/hooks/useAffiliateTags.ts (NEW — will be created)
@src/components/AffiliateDrawer.tsx
</context>

<interfaces>
<!-- Existing AffiliateDrawer props the executor needs -->
From src/components/AffiliateDrawer.tsx:
```typescript
interface AffiliateDrawerProps {
  affiliate:    AffiliateRow | null;
  rankingInfo:  AffiliateRankingInfo | null;
  filteredRows: TransactionRow[];
  onClose:      () => void;
}
```
The drawer uses `affiliate.name` as the affiliate identifier — this is the key for tag lookup.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useAffiliateTags hook</name>
  <files>src/hooks/useAffiliateTags.ts, src/hooks/useAffiliateTags.test.ts</files>
  <behavior>
    - Test 1: addTag("affiliateName", "chris") stores tag in state, getTagsFor("affiliateName") returns ["chris"]
    - Test 2: addTag same tag twice does not duplicate — getTagsFor returns ["chris"] not ["chris","chris"]
    - Test 3: removeTag("affiliateName", "chris") removes it, getTagsFor returns []
    - Test 4: addTag normalizes input: " Facebook " becomes "facebook" (lowercase + trimmed)
    - Test 5: allTags returns deduplicated set of all tags across all affiliates
    - Test 6: empty string or whitespace-only tag is rejected (not added)
    - Test 7: state persists to localStorage under key "affiliview-affiliate-tags"
  </behavior>
  <read_first>
    - src/hooks/useFilters.ts (see existing hook pattern for useState conventions)
  </read_first>
  <action>
    Create src/hooks/useAffiliateTags.ts exporting a single hook useAffiliateTags().

    localStorage key: "affiliview-affiliate-tags"
    Data shape in localStorage: JSON object { [affiliateName: string]: string[] }

    Hook returns:
    - tags: Record<string, string[]> — full tag map (all affiliates)
    - getTagsFor(affiliateName: string): string[] — tags for one affiliate
    - addTag(affiliateName: string, tag: string): void — adds tag (lowercase, trimmed, no duplicates, rejects empty)
    - removeTag(affiliateName: string, tag: string): void — removes tag
    - allTags: string[] — deduplicated sorted array of all tags across all affiliates

    Use useState initialized from localStorage. On every mutation, write back to localStorage via a useEffect or inline in the setter.

    Do NOT use useReducer or external state libraries. Keep it simple with useState + a helper that syncs to localStorage.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useAffiliateTags.test.ts 2>/dev/null || npx jest --testPathPattern=useAffiliateTags 2>/dev/null || echo "If no test runner configured, verify manually: import hook in a component, call addTag, check localStorage in devtools"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useAffiliateTags" src/hooks/useAffiliateTags.ts
    - grep -q "affiliview-affiliate-tags" src/hooks/useAffiliateTags.ts
    - grep -q "addTag" src/hooks/useAffiliateTags.ts
    - grep -q "removeTag" src/hooks/useAffiliateTags.ts
    - grep -q "allTags" src/hooks/useAffiliateTags.ts
    - grep -q "getTagsFor" src/hooks/useAffiliateTags.ts
    - grep -q "toLowerCase" src/hooks/useAffiliateTags.ts
    - grep -q "trim" src/hooks/useAffiliateTags.ts
    - grep -q "export" src/hooks/useAffiliateTags.ts
  </acceptance_criteria>
  <done>Hook file exists with all 4 functions exported. Tags normalize to lowercase/trimmed. localStorage persistence works. Duplicates rejected. Empty strings rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Add tag assignment UI to AffiliateDrawer</name>
  <files>src/components/AffiliateDrawer.tsx</files>
  <read_first>
    - src/components/AffiliateDrawer.tsx (full file — already in context from plan read)
    - src/hooks/useAffiliateTags.ts (just created in Task 1)
  </read_first>
  <action>
    Modify AffiliateDrawer.tsx to add a tag management section:

    1. Import useAffiliateTags from "../hooks/useAffiliateTags"
    2. Inside the component, call const { getTagsFor, addTag, removeTag } = useAffiliateTags()
    3. Add a local state: const [tagInput, setTagInput] = useState("")
    4. Get current tags: const currentTags = getTagsFor(affiliate.name)

    5. Add a new section AFTER the header badges div (line ~110, after aff-drawer-badges), inside aff-drawer-body, as the FIRST section before "Periodo selecionado":

    ```tsx
    <div className="aff-drawer-section">
      <div className="aff-drawer-section-title">Tags</div>
      <div className="aff-drawer-tags">
        {currentTags.map(tag => (
          <span key={tag} className="tag-chip">
            {tag}
            <button
              className="tag-chip-remove"
              onClick={(e) => { e.stopPropagation(); removeTag(affiliate.name, tag); }}
              aria-label={`Remover tag ${tag}`}
            >
              x
            </button>
          </span>
        ))}
        <input
          className="tag-input"
          type="text"
          placeholder="Adicionar tag..."
          value={tagInput}
          onChange={(e) => setTagInput(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === "Enter" && tagInput.trim()) {
              addTag(affiliate.name, tagInput);
              setTagInput("");
            }
          }}
        />
      </div>
    </div>
    ```

    6. Add inline styles or CSS classes for tag-chip, tag-chip-remove, tag-input. Use existing app styling conventions (var(--bg-2), var(--text-2), etc.). Tag chips: small rounded pills with background var(--bg-2), font-size 12px, padding 2px 8px, border-radius 4px. Remove button: no border, cursor pointer, margin-left 4px, font-size 10px. Input: no border, background transparent, font-size 12px, flex-grow, outline none.

    The tags section should be compact — it's informational, not the main content.
  </action>
  <verify>
    <automated>cd /Users/victorroque/Documents/Claude-Code/Affiliview-v3 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useAffiliateTags" src/components/AffiliateDrawer.tsx
    - grep -q "tag-chip" src/components/AffiliateDrawer.tsx
    - grep -q "tag-input" src/components/AffiliateDrawer.tsx
    - grep -q "addTag" src/components/AffiliateDrawer.tsx
    - grep -q "removeTag" src/components/AffiliateDrawer.tsx
    - grep -q "Adicionar tag" src/components/AffiliateDrawer.tsx
  </acceptance_criteria>
  <done>AffiliateDrawer shows a Tags section with existing tags as removable chips and an input field. User can add tags by typing + Enter and remove by clicking X. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage read/write | User-controlled storage, no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | localStorage tag data | accept | Tags are user-assigned display labels with no security impact. Tampered data only affects user's own view. |
| T-07-02 | I (Info Disclosure) | localStorage | accept | No PII stored — only affiliate names (already visible in UI) and user-created tag strings. |
</threat_model>

<verification>
1. Open app, navigate to Affiliates page
2. Click any affiliate row to open the drawer
3. In the Tags section, type "chris" and press Enter — tag chip appears
4. Type "facebook" and press Enter — second chip appears
5. Click X on "chris" chip — it disappears
6. Close drawer, refresh page, reopen same affiliate — tags still present
7. TypeScript compiles: npx tsc --noEmit
</verification>

<success_criteria>
- useAffiliateTags hook created with addTag, removeTag, getTagsFor, allTags
- AffiliateDrawer shows tag assignment UI
- Tags persist in localStorage under key "affiliview-affiliate-tags"
- Tags normalized to lowercase and trimmed
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-tags-de-afiliados/07-01-SUMMARY.md`
</output>
