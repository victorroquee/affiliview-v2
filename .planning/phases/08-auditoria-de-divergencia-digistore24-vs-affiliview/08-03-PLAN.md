---
phase: 08-auditoria-de-divergencia
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/pages/Dashboard.tsx
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-05

must_haves:
  truths:
    - "Dashboard Gross Revenue KPI tooltip reflects updated scope (all payments, not front-only)"
    - "Dashboard info text no longer claims front-only alignment"
  artifacts:
    - path: "src/pages/Dashboard.tsx"
      provides: "Updated KPI tooltip text"
      contains: "front + upsells"
  key_links:
    - from: "src/pages/Dashboard.tsx"
      to: "PeriodMetrics.gross"
      via: "metrics.gross display"
      pattern: "metrics.gross"
---

<objective>
Update the Dashboard KPI card tooltip text to reflect the corrected Gross Revenue and Earnings definitions (all payments, not front-only).

Purpose: The KPI info tooltips currently state "Receita bruta dos pedidos frontais (upsell_no=0)" which is no longer accurate after Plan 02's fix. Users reading the tooltip would get incorrect information about what the number represents.

Output: Updated tooltip strings in Dashboard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-auditoria-de-divergencia-digistore24-vs-affiliview/08-RESEARCH.md

<interfaces>
From src/pages/Dashboard.tsx (current tooltip, line 136):
```typescript
info="Receita bruta dos pedidos frontais (upsell_no=0). Alinhado com o gross do dashboard Digistore24. Upsells e bumps são contabilizados no AOV mas não inflam este KPI."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Gross Revenue and Earnings KPI tooltip text</name>
  <files>src/pages/Dashboard.tsx</files>
  <read_first>src/pages/Dashboard.tsx</read_first>
  <action>
In `src/pages/Dashboard.tsx`, find the KPI card for Gross Revenue (around line 135-136) and update its `info` prop.

Replace the Gross Revenue info text:
```
"Receita bruta dos pedidos frontais (upsell_no=0). Alinhado com o gross do dashboard Digistore24. Upsells e bumps são contabilizados no AOV mas não inflam este KPI."
```
With:
```
"Receita bruta de todos os pagamentos (front + upsells + bumps). Alinhado com Gross Amount do dashboard Digistore24."
```

Also check if there is an info tooltip for the Earnings KPI card (around line 142-143). If it mentions "front-only" or "pedidos frontais", update it to reflect that Earnings now includes all payment types. The new text should be:
```
"Ganhos do produtor de todos os pagamentos (front + upsells) menos deducoes de reembolsos e chargebacks. Alinhado com Your Earnings do dashboard Digistore24."
```

Also check the Daily Gross chart section — if there is a comment or label referencing "front-only", update it to "all payments".
  </action>
  <verify>
    <automated>grep -c "front + upsells" src/pages/Dashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "front + upsells" src/pages/Dashboard.tsx returns at least 1 match
    - grep "pedidos frontais (upsell_no=0)" src/pages/Dashboard.tsx returns 0 matches (old text removed)
    - grep "Gross Amount do dashboard Digistore24" src/pages/Dashboard.tsx returns 1 match
    - No TypeScript compilation errors: npx tsc --noEmit
  </acceptance_criteria>
  <done>Dashboard KPI tooltips updated to accurately describe the corrected Gross (all payments) and Earnings (all payments + refund/CB) scope, aligned with Digistore24 definitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — text-only change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | I (Information Disclosure) | Tooltip text | accept | Tooltip text is user-facing informational only; no secrets or sensitive data |
</threat_model>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. Open Dashboard, hover over Gross Revenue KPI card — tooltip says "todos os pagamentos (front + upsells + bumps)"
3. No reference to "pedidos frontais (upsell_no=0)" remains in Dashboard.tsx
</verification>

<success_criteria>
- Gross Revenue tooltip accurately describes all-payment scope
- Earnings tooltip accurately describes all-payment scope
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-auditoria-de-divergencia-digistore24-vs-affiliview/08-03-SUMMARY.md`
</output>
